# 12 — Dead code and compiler warnings

> *This was generated by AI during triage.*

Status: done
Category: bug
Source: ISSUES.md #12 (verified 2026-08-20: CONFIRMED)

## Agent Brief

**Category:** bug
**Summary:** Remove confirmed-dead fields, types, and the unused code behind the 17 compiler warnings.

**Current behavior:**
`cargo check` emits 17 warnings. Confirmed dead: an unused `Game` import in the
binary; the `Player::new` rng parameter it never uses; an unused `ignored`
variable in monster deserialization; an unused `rarity` in loot rolls; the
`Game::amulet_taken` field (never set or read after init); the `Tombstone`
type and `tombstones` field (never constructed).

**Desired behavior:**
The dead items are deleted (or genuinely wired up if a reviewer decides one of
them is load-bearing — default is delete). `cargo check` completes with zero
warnings.

**Key interfaces:**
- The binary's imports: drop the unused `Game` import
- `Game`: delete `amulet_taken` and the tombstone machinery unless a sibling
  issue needs them first
- `Player::new`: drop or use the rng parameter per the other entity constructors
- The loot `roll_drop`: drop or use the `rarity` parameter

**Acceptance criteria:**
- [ ] `cargo check` (lib + bin) completes with zero warnings
- [ ] No behavior change for live paths; existing tests pass
- [ ] No removed symbol is referenced anywhere

**Out of scope:**
- Wiring in tombstones or amulet-taken logic as features (if ever wanted, they
  are separate enhancement tickets)

## Comments

> *This was generated by AI during triage.*

- Verified 2026-08-20: all 17 warnings reproduce; every cited symbol is
  confirmed dead. Note: some of the unused variables disappear as siblings land
  (e.g. `ignored` goes away with issue 03) — this ticket is the sweep that
  leaves zero warnings.

- Resolved (a93629d): deleted `amulet_taken`, the `Tombstone` type, and
  `tombstones`; dropped the unused `rng` param from `Player::new` (now matches
  `Monster::new`) and the unused `rarity` param from `roll_drop`; removed the
  dead imports and the stray `mut`/unused vars. `cargo check --all-targets` is
  warning-free and all 54 tests pass.
