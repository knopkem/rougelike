# 15 — Quests can never progress

> *This was generated by AI during triage.*

Status: needs-triage
Category: enhancement
Source: ISSUES.md #15 (verified 2026-08-20: CONFIRMED)

## Triage Notes

**What we've established so far:**

- Verification confirmed: no Offered→Active acceptance flow exists; the kill
  counter for quest 2 only increments when the quest is already Active, so it
  can never start; the progress-check hook is an empty body; no rewards are
  granted; no quest-specific items exist (signet ring, iron key, sealed
  vault); the quest log is never rendered (no panel variant); and the three
  quests are defined in two places (`QuestLog::default` and
  `data::quests::initial_quests()`, the latter of which is never called).
- The core quest state machine (states, counters, progress hook) is in the
  quest module and independent of the UI work in issue 13.

**Open questions (maintainer, before this becomes an agent brief):**

1. Quest acceptance: automatic when the player picks up / interacts with
   quest items, or via a quest-giver NPC (PLAN mentions givers; that implies
   NPC work which no module currently has)?
2. Source of truth: `data::quests::initial_quests()` (data-driven, matches
   the rest of the game) or `QuestLog::default()` — which do we keep?
3. Rewards: which of the PLAN's rewards (potions/XP/wands/legendaries) ship
   in the first cut?
4. Scope of the first cut: just the quest log panel + kill/progress tracking,
   or also quest-specific items (signet ring, iron key, sealed vault — the
   vault overlaps issue 20)?

Once answered, this moves to `ready-for-agent` with a brief.

## Comments

> *This was generated by AI during triage.*

- Verified 2026-08-20: all five sub-claims in ISSUES.md hold. Marked
  `needs-triage` rather than `ready-for-agent` because the four open questions
  are design decisions, not implementation detail.
