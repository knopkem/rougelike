# 21 — Endless mode not implemented

> *This was generated by AI during triage.*

Status: done
Category: enhancement
Source: ISSUES.md #21 (verified 2026-08-20: CONFIRMED)

## Agent Brief

**Category:** enhancement
**Summary:** After taking the amulet the run hard-ends; the planned optional endless descent is unreachable, though its scaling plumbing already exists.

**Current behavior:**
The `Game::endless` flag is initialized false and never set. Descending caps
at the max depth constant, and victory ends the run at the amulet. The
depth-beyond-max monster scaling branch in spawning exists but is dead.

**Desired behavior:**
When the player picks up the amulet, they may continue descending: generation
keeps producing levels beyond the old cap using the existing depth-scaling
spawn branch, and the old victory path (run ends) remains the alternative
behavior per PLAN (endless is optional — confirm the trigger: prompt on
pickup, or a flag).

**Key interfaces:**
- `Game::endless`: set at the amulet pickup decision point
- The descend handler: lift the max-depth cap while endless is active
- The victory check: only fires when the player chose to end the run
- Spawn scaling branch: already handles depths beyond the cap; wire it live

**Acceptance criteria:**
- [x] Picking up the amulet offers the endless choice (or the trigger decided
      during implementation review of PLAN); declining ends the run as today
- [x] Choosing endless allows descending past the old max depth
- [x] Monsters beyond the cap use the existing scaling branch
- [x] The run still ends normally if the player declines endless

**Out of scope:**
- New content tiers beyond the existing scaling branch
- Endless-specific UI (beyond the prompt)

## Comments

> *This was generated by AI during triage.*

- Verified 2026-08-20: flag never set; cap enforced in the descend handler;
  scaling branch exists but is unreachable. Small change with one design
  point: the exact trigger for offering endless (PLAN says "optional" — a
  prompt at pickup is the natural reading).
- Resolved (638074a): the trigger is the victory screen, per PLAN §5/§6
  ("victory screen ... then endless-continue or new run"): the amulet pickup
  still fires the victory screen (score, hiscore, autosave deletion — all
  unchanged), which now offers `[C]ontinue in endless mode   [Enter] return
  to title`. `App::continue_endless` sets `Game::endless` and resumes play;
  `try_stairs_down` lifts the `MAX_LEVELS` cap while `endless` is set
  (checked_add guards overflow) and `maybe_spawn`/`spawn_monster` scale
  D26+ spawns via the pre-existing `endless && depth > 25` branch (+5 HP and
  +10 XP per depth past 25). Declining (Enter) ends the run exactly as
  before. No autosave is restored — won runs are excluded by design. 127
  tests pass, including descent-past-cap, spawn-scaling, and
  victory-screen-continue coverage.
