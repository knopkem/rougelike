# 23 — No integration tests, no CI

> *This was generated by AI during triage.*

Status: ready-for-agent
Category: enhancement
Source: ISSUES.md #23 (verified 2026-08-20: CONFIRMED)

## Agent Brief

**Category:** enhancement
**Summary:** The library exposes a test harness nothing uses, there is no integration test suite, no CI workflow, and `proptest` is an unused dev-dependency.

**Current behavior:**
`lib.rs` exposes a test-harness entry point (a fresh game from a seed) with no
callers. There is no integration test directory and no CI workflow. Unit
tests do exist inline in a few modules (FOV, path, message, RNG, combat,
synth) — the gap is integration coverage, not all testing.

**Desired behavior:**
Per PLAN, an integration suite in the integration-tests directory driving the
library harness: generation invariants, combat, inventory, save/load
round-trip, and a simulated full game (headless, per issue 22). Property
tests via `proptest` where they fit (generation invariants, RNG). A CI
workflow running `cargo check` + `cargo test` (and clippy/fmt if cheap).

**Key interfaces:**
- The library test harness: the entry point for building games from a seed
- The headless mode from issue 22 (if it lands first) as the driver for the
  simulation suite; if not, drive `do_turn` directly
- CI: a workflow file running the standard Rust CI steps

**Acceptance criteria:**
- [ ] Suites exist and pass: gen_invariants, combat, inventory,
      save_roundtrip, sim_game (per PLAN)
- [ ] At least one `proptest` property test in the suite
- [ ] The save_roundtrip suite would have caught the bug in issue 03
- [ ] CI workflow runs check + tests and is green on the merged state
- [ ] The harness entry point is actually used by the suites

**Out of scope:**
- Unit test additions beyond what the suites need
- Coverage tooling

## Comments

> *This was generated by AI during triage.*

- Verified 2026-08-20: no integration test dir, no CI, harness unused,
  proptest unreferenced. Note: inline unit tests do exist, so this is
  "no integration tests", not "no tests". The repo has no remote yet; the CI
  workflow file is the deliverable and activates when the repo is hosted.
