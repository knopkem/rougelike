# 16 — Shop is dead code

> *This was generated by AI during triage.*

Status: needs-triage
Category: enhancement
Source: ISSUES.md #16 (verified 2026-08-20: CONFIRMED)

## Triage Notes

**What we've established so far:**

- Verification confirmed: `shop.rs`'s price and buy/sell functions have zero
  call sites; there is no merchant NPC, no shop panel variant, and the core's
  buy/sell actions are unreachable (issue 13). Gold currently has no sink.
- A full shop also needs a D2 shop room (issue 20) and key handling in the
  style of issue 13.

**Open questions (maintainer, before this becomes an agent brief):**

1. Merchant model: a wandering merchant NPC, or a fixed shop room on D2 (PLAN
   mentions a D2 vault)? What is the inventory/stock model — fixed list, or
   generated?
2. UI: dedicated shop panel (price column, buy/sell quantity) or reuse the
   inventory panel?
3. Pricing: the existing `buy_price`/`sell_price` functions define a formula —
   keep it as-is?
4. Scope of the first cut: buy-only, or buy+sell?

Once answered, this moves to `ready-for-agent` with a brief.

## Comments

> *This was generated by AI during triage.*

- Verified 2026-08-20: the entire shop module is uncalled. Marked
  `needs-triage` — the pricing math exists but the merchant, stock, UI, and
  map-room design are open.
